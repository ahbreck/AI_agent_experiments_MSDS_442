# AI_agent_experiments_MSDS_442

Primary setup and run instructions for the current prototype are in:

- `Project_Phase_3/PROTOTYPE_README.md`

Environment is managed via `environment.yml` at the repository root.
